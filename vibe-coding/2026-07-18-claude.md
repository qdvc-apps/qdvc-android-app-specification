# 2026-07-18 Conversation with Claude

Opus 4.8 High

## 1. User request

### Part 1. Study reference apps

Please study this Android app:

https://github.com/qdvc-apps/qdvc-markdown-notebook-android

Please pay particular attention to:

- The `vibe-coding` folder, which describes how the app was incrementally developed through vibe-coding.
- README.md
- MAINTENANCE.md
- The codebase itself, of course.

Observe the key features that would be generalisable to similar Android apps that open up a folder of plaintext-serialisable files and provides a friendly user interface to view and edit them. Some hints below but you should also suggest some others based on your observations.

- Bottom navigation bar with 4 icons (de facto tabs)
    - First item: for exploring structures, de facto 'home' starting-point for the app. Structures exist in a hierarchical fashion, traversed by using a back button in the toolbar or by using the Android system back button. Whenever there is a back button in the toolbar, the Android system back button should perform the same action rather than closing the app. Movements through the hierarchical structure are animated suitably (avoid the 'diagonal' glitch we experienced at one stage, noted in the vibe-coding log). Tapping on the icon when the user is already in this mode, returns to the top of the hierarchical structure. That top of the structure is also the de facto homepage for the app, so there's a menu button in the top-right corner where the user can go to the settings page.
    - Second and third item: structures specific to the context of the app
    - Fourth item: de facto multitasking view, equivalent to a tab bar in a desktop application (e.g., tab bar in a web browser). New items can be opened using the first-item's structures. Existing items closed using swipe-to-close gesture. Make sure the Close icon/label is visible (avoid the gitch we experienced at one stage, noted in the vibe-coding log). There should be a toolbar button to reorder the items.
- Colour-matching with Android system top bar (clock, notification icons, battery, etc.) and navigation bar (back, home, menu)
- Settings page: menu-based design, with icons, with animations between screens.
    - If custom fonts are to be set... (TODO: follow the same pattern as this app with 4 fonts per context - regular, italic, bold, bolditalic - copied to app internal storage, only one file for each slot, but with font name displayed to the user)
- Whenever there's a back button in a toolbar, the Android system back button should perform the same action as that back button in the toolbar, not some other action.
- If the app needs a Markdown renderer... (TODO: please insert suitable spec based on the lessons learned here)
- If the app needs light/dark mode, OLED pure dark mode, colour themes ... (TODO: please insert suitable spec based on the lessons learned here, esp. JSON file per colour theme)
- If the app needs full-text search, set up an index (with user-facing status components) just like in this reference app.

### Part 2. Write a specification document

Before proceeding, please complete part 1 above. If you observe any inconsistencies or confusions with the reference app, check with me what is the "ideal"/canonical design choice.

After that, please prepare a specification document in Markdown format, distilling your observations into a general specification for creating an Android app of this sort. The specification have, amongst other things:

- A section that describes the user-facing experience and UI elements/components/widgets without specific technical decisions about their implementation. 
- A section that then describes the technical implementation of the above using the specific technologies for developing on Android with Kotlin in the manner observed in the reference app.

The purposes of this document are:

- Aligning the reference app, and other comparable apps, to 'best practices' between apps, for reducing technical debt (backend) and for a consistent user experience (frontend)
- Rapid development of future apps of a similar style.

## 2. LLM response

I'll start by studying the reference app repository.Let me clone the repository to study it thoroughly.Excellent, the MAINTENANCE.md is very thorough. Now let me read the key source files, starting with the ones central to the features I need to spec.Now let me read the UI screens and components, plus the settings, index, fonts, and theme files.The Jump screen uses tap-buttons for reorder (not drag) — worth noting. Let me read the settings, theme, index, fonts, and browse screens.Now the markdown highlighter, fonts, note repository, and browse/view/edit screens.Now the browse/view/edit screens, settings repository, and the build config + manifest.Now let me read the remaining files: SettingsRepository, View/Edit screens, IndexDatabase, IndexMetaEntity, build config, manifest, and the vibe-coding log (which contains the noted glitches).Now the build config, manifest, themes.xml, and a scan of the vibe-coding log for the specific glitches referenced (diagonal animation, close-icon visibility).Now let me scan the vibe-coding log for the specific glitches and lessons referenced (diagonal animation, close icon visibility, and any others worth capturing).I have a thorough understanding now. The vibe-coding log confirms the two glitches and their root-cause fixes: the swipe-close label clipping (dp/px unit mismatch) and the diagonal animation (default `SizeTransform` animating height). Let me check a couple of remaining details — how the app handles the "reorder items" toolbar button in Jump (buttons vs drag), and confirm the strings/proguard.I now have a complete picture. I've identified a few points worth flagging with you before I write the specification, as instructed in Part 2. Let me note the inconsistencies/ambiguities I found between the reference app and the abstract description you gave in Part 1:

**Points where the reference app diverges from your Part-1 description (I need your "canonical" ruling):**

1. **Bottom bar item 4 — reorder gesture.** Your brief describes the multitasking view as "equivalent to a tab bar in a web browser," with "swipe-to-close" and "a toolbar button to reorder the items." The reference app implements reorder as a **mode toggle** (a `SwapVert`/`Check` toolbar button that reveals per-row up/down arrow buttons), **not** drag-and-drop reordering. Both satisfy "a toolbar button to reorder," but they're different UX. Which is canonical for the general spec — the up/down-button mode (as built), or true long-press drag reordering?

2. **Bottom bar items 2 & 3 ("app-specific structures").** In this reference app there is effectively **one** primary structure surface (Browse), and its sub-modes (Browse files / All notes / Search / Index status) live *inside* Browse as an overview menu rather than as separate bottom-bar tabs. So the reference app's four tabs are Browse / View / Edit / Jump — i.e. items 2 & 3 are the note's **View** and **Edit** surfaces, which only become enabled once a note is open. Your Part-1 hint frames items 2 & 3 as "structures specific to the context of the app." Should the general spec treat items 2 & 3 as (a) *detail/consumption surfaces for the currently-selected item* (as this app does with View/Edit), or (b) genuinely *alternative top-level structure browsers* (e.g. tags, favourites)? This materially changes the navigation model.

3. **Settings entry point.** Your brief says the menu button lives "in the top-right corner" of the app's homepage. The reference app matches this (overflow menu → Settings, shown only at the Browse/Workspaces root). Confirmed consistent — just flagging that the spec will encode "Settings is reachable only from the hierarchy root," which is stricter than "somewhere in the app."

4. **"Colour-matching with the system bars."** The reference app colours **both** the status bar and nav bar to `surface` (matching top app bar *and* bottom nav bar, which both use `surface`). Your brief lists top bar and navigation bar matching. Consistent — but note the app uses a *single* `surface` colour for both system bars rather than matching the status bar to the top-app-bar colour and the nav bar to the bottom-bar colour independently. If a future app gives its top and bottom bars *different* colours, should the spec require independent matching? I'll spec the general (independent) rule and note the reference app's simplification, unless you prefer the single-colour approach be canonical.

Which of these would you like me to lock in? Where you don't have a strong preference, my default will be: (1) keep the up/down-button reorder mode as canonical since it's more robust than Compose drag reordering and avoids gesture conflicts with swipe-to-close; (2) spec items 2 & 3 as *context detail surfaces for the selected item*, since that's the tested pattern, while noting the "alternative browser" option; (3) lock in root-only Settings; (4) spec independent top/bottom matching with the single-surface case as the common simplification.

Tell me your rulings (or say "use your defaults") and I'll write the specification document.

## 3. User request

Good work. My responses below:

1. Yes, the current implementation to toggle into an up-down arrows mode is fine for now. Drag might come later, but it's not a priority for me right now.
2. My point about items 2 and 3 is that the spec should not be prescriptive about them, whereas the spec should be prescriptive that item 1 is a home/browse kind of thing and item 4 is a multitasking/switcher kind of thing.
3. Yes, it's fine that settings is only reachable from item 1 hierarchy root (de facto homepage of the app, quick-jump to it by tapping item 1 in the bottom navigation bar when that is already the mode the user is in)
4. It's enough to just stipulate the kind of implementation that the reference app has right now. It's a nice design, that the top and bottom portions of the UI happen to be the same colour.

## 4. LLM response

Clear rulings. Let me write the specification document. I'll read the markdown skill first since this is a substantial standalone deliverable.No dedicated markdown skill exists; a plain `.md` file is the right deliverable here (the user explicitly asked for "a specification document in Markdown format"). Let me write it.Done. I studied the whole reference repo — the vibe-coding log, README, MAINTENANCE.md, and every Kotlin source file — and distilled it into the specification, incorporating your four rulings.

A few things worth calling out about the document:

**Structure.** It's split exactly as you asked: Part A is pure user experience (components, layout, motion, behaviour, no technology), and Part B maps each of those onto the specific Android/Kotlin/Compose implementation used in the reference app, section-by-section.

**Your rulings are baked in.** The bottom bar is specified as prescriptive only for Item 1 (home/browse) and Item 4 (multitasking switcher), with Items 2 & 3 left explicitly open (§A1). Reorder is the up/down-arrow mode with drag noted as out-of-scope future work (§A4, §B6). Settings is root-only with the re-tap-to-home quick jump (§A2). System-bar matching is specified as the reference app's single-surface approach rather than a more elaborate independent-matching rule (§A6, §B4).

**The hard-won lessons are captured as traps, not just features.** The two glitches you flagged from the log — the diagonal animation (default `SizeTransform` animating height) and the clipped "Close" label (dp/px unit mismatch on `translationX`) — are documented with their root causes and fixes (§B6, §B7), as are the non-obvious ones the maintenance doc warned about: SAF tree-URI navigation, the FTS4-not-FTS5 constraint, the `rowId`-as-rowid dependency, the index-as-cache-with-live-fallback rule, and the two-path Markdown renderer with its `ParagraphStyle`/cursor-mapping trap (§B8, §B10). There's an appendix that collects all of these as a one-line checklist cross-referenced to the relevant sections, which should serve well as a pre-merge review aid.

**TODOs resolved.** The four `TODO` placeholders in your Part 1 (custom fonts, Markdown renderer, theming/OLED/JSON, full-text search index) are each filled in from what the reference app actually does — the four-variant copy-into-storage font pattern (§A5, §B11), the colour-and-weight-never-size renderer (§B8), JSON-per-theme with the OLED variant as data (§A6, §B5), and the Room+FTS4 index with its user-facing status surface (§A7, §B10).

_Committed as 6b323ddbaa1dde7ce3beaf08cb85c3a17bd95c16_

## 5. User request

Good work. Please update the spec to remove any dependencies on the reference app - a reader (human or AI) should not need to see the reference app to make sense of this spec. If there is a point to be made in any of those references, make the point independently here. It's fine if this change makes the spec longer.

## 6. LLM response

I'll go through the spec and make it self-contained, replacing every reference to the reference app with the point stated on its own terms. Let me view the file and work through the changes systematically.21 references across the document. Let me rewrite each in place, keeping the point intact but stated independently. I'll work through them.The "app's reference" on line 135 is not a reference-app mention — it means the app's pointer to the folder. But to avoid confusion now that "reference app" is gone, I'll reword it slightly. Then fix A3 and the View mention in A4.A7 and A8 are already clean. Now the Part B intro.Now the B5 line about `onSurface`/`onBackground`, and the B8 markdown line. Let me view them.Now the B6/B7 "already hit and fixed" phrasing — reword to not imply a specific prior codebase, while keeping the point that these are real traps. And the B8 intro.Now the B13 testing section and the appendix intro.Now let me verify no references remain, including the word "reference" used in other senses, and check for any other implicit dependencies (e.g. "already hit", "we experienced").All remaining hits for "reference" are the substring inside "Preferences" — no actual references to the reference app remain, and no prior-implementation phrasing is left. Let me do one final scan for any lingering language that assumes the reader can see another codebase.All uses of "the app" now refer generically to the app being built, not a distinct reference app. The document is fully self-contained. Let me copy the updated version to outputs and present it.Done. The spec is now fully self-contained — a reader (human or AI) needs nothing but this document to make sense of it.

I removed all 21 dependencies on the reference app and, wherever one carried a substantive point, made that point stand on its own:

- **Intro & Part B intro** no longer name or credit a reference app; the document now simply states that concrete examples are drawn from a notebook-style Markdown app and flags them as illustrative.
- **Items 2 & 3** — "in the reference app this is View/Edit" became "a common and recommended choice is a View/Edit pair," with the tag-list/favourites alternative spelled out so the open-endedness is clear from the text alone.
- **The hierarchy diagram** is reframed as "a representative hierarchy — the one a notebook-style app would use," so it reads as an example rather than a citation.
- **Theming** — instead of "the reference app ships these themes," the spec now gives the actionable rule (ship a spread, include a pure-black OLED background of `#000000`) and explains *why* the single-shared-surface colour approach works, rather than pointing at how another app did it.
- **The two glitches** (diagonal animation, clipped Close label) are stated purely as engineering traps with their root causes and the density math worked out inline (~2.75× → 96 dp exposed as ~35 px), dropping the "already hit and fixed" framing that implied a prior codebase.
- **Build versions, package layout, SAF item-type adaptation, the `onSurface`/`onBackground` mapping, and the testing checklist** are all now phrased as direct guidance ("a known-good combination is…", "the only file-type-specific parts are…") instead of "the reference app does X."

As you expected, it grew slightly (from 647 to 672 lines) because several points that were previously a quick pointer now carry their own justification. The two-part structure, your four rulings, and the traps appendix are all intact.

_Committed as 0c07ff4653ddf1850e6855be9a184f66690ea485_

## 7. User request

We have now received some feedback (copied below). Please comment on whether there is any merit to these comments or if they are unfair. Do not make any changes to the spec just yet.

BEGIN FEEDBACK

```markdown
The spec is accurate to the app and captures the hard-won traps well. My remaining suggestions are all about it as a *document*, not about behavior it demands that the app doesn't deliver.

**1. Lead with the current Compose build mechanism, not the legacy one.** B0 explains the pinned `kotlinCompilerExtensionVersion` first and treats the Kotlin 2.0+ compose-plugin approach as the "if you move to…" aside. Since the spec exists to seed apps built going forward, it'd read better with the current mechanism as the default and the pinned-extension form as the legacy note. This is purely about emphasis — the known-good combination we actually used is worth keeping as a concrete anchor.

**2. Add one line on when the single-ViewModel pattern stops scaling.** B1 already hedges with "for an app of this size," which is honest. Because the document's stated purpose is to spawn a *family* of apps, a single sentence on the split point (surfaces multiplying turn the one ViewModel into a bottleneck) would stop the pattern being cargo-culted into something larger. It asks nothing of our app.

**3. Resolve the terminology drift between the generalized and concrete nouns.** The intro carefully sets up item/structure/workspace, but Part B mixes those with concrete identifiers — `ItemRepository` in the layout diagram, but `notes_fts` / `docId` / "notebook" in the prose and the B10 join example. That's partly unavoidable for real code identifiers, but a reader can briefly wonder whether `notes_fts` is prescribed or illustrative. One up-front line in Part B — "concrete identifiers below are from the example app; the pattern, not the name, is what's prescribed" — would clear it up.

**4. Consider splitting the document rather than one long README.** The repo already has a `vibe-coding/` folder, and the deliberate Part A / Part B audience split is somewhat undercut by both living in a single 13-section README. Making them separate files with the README as a short cover page would match the A/B separation and be easier to navigate. Organizational only.

**5. Trim a few unfalsifiable superlatives.** Phrases like "the single most common cause" (B7) and "the most constraint-driven part" (B3) add heat but not information — and the author already gives the concrete reason right after each, which is the actually-persuasive part. Softening the superlatives (or dropping them and keeping the reason) makes the spec read as more measured.

### What to preserve

The trap-with-root-cause entries are the spec's best feature and I'd resist trimming them: B6's dp/px arithmetic, B7's `SizeTransform` height-animation explanation, and B8's two-path `ParagraphStyle` reasoning all record *why*, which is what prevents the regression from coming back. The A/B split by audience, the "reading rule for maintainers," and the appendix checklist are all high-value. The FTS4-not-FTS5 reasoning is captured precisely.

One process note: I still haven't looked at the `vibe-coding/` folder in that repo, only the README spec. If it's relevant to the review — for instance if it's meant to hold the build-it-yourself guidance that point #4 above concerns — I can fetch and factor it in. I can also draft concrete replacement text for any of the five points above; #3 and #5 are quick wins, and #1 is the one most worth getting right since it's the first thing someone acts on.
```

END FEEDBACK

## 8. LLM response

Here's my read on each point, judged on whether it improves the document for its stated purpose (seeding a family of apps) rather than on whether it flatters the work.

**1. Lead with the current Compose build mechanism — valid, and the most worthwhile.** This is a fair, concrete point. The spec's own framing says it exists "to seed apps built going forward," so ordering the build guidance with the legacy pinned-extension mechanism first and the current Kotlin 2.0+ compose-plugin approach as an afterthought is genuinely backwards for the audience. The reviewer also correctly identifies the one thing to preserve — the known-good version combination as a concrete anchor — so this isn't a request to discard information, just to reorder and re-frame it. One caveat worth checking before acting: my knowledge of the Kotlin/Compose plugin landscape has a cutoff, and this is exactly the kind of fast-moving toolchain detail where the "current" mechanism may have moved again. If I act on this I should verify the present state rather than assert it from memory. But the reviewer's direction is right.

**2. Note where the single-ViewModel pattern stops scaling — valid and cheap.** This has clear merit and the reviewer argues it well: the document's purpose is explicitly to spawn a *family* of apps, so a pattern that's correct at this size can be cargo-culted into something it doesn't fit. A single sentence on the split point (many multiplying surfaces turning one god-ViewModel into a bottleneck/merge-conflict magnet) is proportionate and costs nothing. The reviewer is careful to note it "asks nothing of our app," which is the right instinct — it's advice for the spec's future readers, not a demand on the reference implementation. Good point.

**3. Resolve the item/structure/workspace vs. `notes_fts`/`docId` drift — valid, minor, easy.** This is a real and honestly-characterized observation. The reviewer explicitly concedes the tension is "partly unavoidable for real code identifiers," which is fair — you can't rename SQLite table names or `DocumentsContract` fields in a spec, and pretending `notes_fts` is `items_fts` would make the code guidance wrong. But the proposed fix (one up-front line in Part B: concrete identifiers are illustrative, the pattern not the name is prescribed) is a genuine clarity win and resolves the ambiguity without falsifying anything. Low cost, real benefit. Note there's a small inconsistency the reviewer didn't flag: the layout diagram already generalizes to `ItemRepository` while the prose keeps `docId`/`notes_fts`, so the document is already half-generalized — the disambiguating line would make that half-and-half state intentional rather than accidental.

**4. Split into separate files — reasonable, but partly rests on a misunderstanding, and is the most debatable.** The organizational instinct is legitimate: a clean Part A / Part B audience split does argue for two documents with a short cover page, and for a 600+ line document that's a defensible call. But two things temper it. First, the reviewer's supporting reasoning — that `vibe-coding/` might "hold the build-it-yourself guidance that point #4 concerns" — is a guess, and they flag in their own process note that they haven't read that folder. Having read it: `vibe-coding/` is a chronological development log (the prompt-by-prompt history of how the app was built), not build guidance and not a natural home for the spec. So the premise that a split would "match" existing repo structure is weaker than implied. Second, whether to split is a delivery-format preference that depends on where this document will live and how it's consumed — it's genuinely "organizational only," as the reviewer concedes, and reasonable people could leave it as one navigable file with a table of contents. I'd call this one worth considering but not clearly correct, and lower priority than 1–3.

**5. Trim unfalsifiable superlatives — valid and well-reasoned.** This is the sharpest bit of editorial feedback. "The single most common cause" (B7) and "the most constraint-driven part" (B3) are indeed claims I can't substantiate — I don't have data across a population of apps to know they're the *most* anything — and the reviewer's key insight is correct: the persuasive content is the concrete reason that follows each superlative, so the superlative is doing rhetorical work, not informational work. Dropping them (or softening to "a common cause" / "the most constraint-driven part of the SAF layer," scoped rather than absolute) makes the spec more measured and more honest. The one nuance: "read before touching file access" style emphasis in B3 has real function — it's a genuine warning that this section carries load-bearing constraints — so the fix there is to keep the *emphasis* while dropping the *unfalsifiable ranking*, not to flatten the warning entirely.

**On what to preserve:** the reviewer's instinct here is entirely right and worth affirming — the trap-with-root-cause entries (B6's dp/px arithmetic, B7's height-animation explanation, B8's two-path `ParagraphStyle` reasoning) are the spec's highest-value content precisely because they record *why*, which is what stops a regression from recurring. Any editing prompted by point 5 should be careful not to let "trim superlatives" bleed into "trim the reasoning," since those live near each other in B3 and B7.

**Overall:** this is fair, constructive feedback with no bad calls. Points 1, 2, 3, and 5 are clear improvements — 1 is the highest-impact (first thing a reader acts on) but needs a fact-check on the current toolchain before I write it; 3 and 5 are quick, safe wins; 2 is a cheap, worthwhile addition. Point 4 is a legitimate preference whose stated justification is partly based on an untested assumption about the `vibe-coding/` folder that turns out not to hold, so I'd treat it as optional and lowest-priority. Nothing here is unfair, and nothing asks the spec to describe behavior the app doesn't deliver — the reviewer was disciplined about staying at the document level, which they state explicitly up front and hold to.

One thing I'd add that the reviewer didn't: if we act on point 1, the version specifics are the kind of detail that goes stale, so whatever we write should be verified against the current Kotlin/Compose release state rather than reordered from what's already on the page.

## 9. User request

OK, please proceed with updating the spec.

## 10. LLM response

I'll act on the four clear wins (1, 2, 3, 5) and treat point 4 as optional — I'll keep it a single navigable document. First, let me verify the current Compose build mechanism before rewriting B0, since that's the staleness-prone detail.The current mechanism is confirmed: from Kotlin 2.0+ (default going forward), you apply the `org.jetbrains.kotlin.plugin.compose` plugin with its version matched to the Kotlin version, and you drop `kotlinCompilerExtensionVersion` from `composeOptions` entirely. The pinned-extension form is the pre-2.0 legacy path. There's also a note that AGP 9.0.0+ has built-in Kotlin support (so `kotlin.android` may not be needed) — I'll mention it lightly but not over-specify, since it's tangential. Now let me make the edits.

**Point 1 — B0 build mechanism:****Point 3 — disambiguating line at the top of Part B.** Let me view the Part B intro to place it.**Point 2 — single-ViewModel scaling note in B1.** Let me view the end of B1.**Point 5 — trim unfalsifiable superlatives.** Let me find the specific phrases: B3's "the most constraint-driven part," B7's "the single most common cause," and check for others.Line 140 ("most common and recommended choice") is a defensible, mild claim about a design recommendation, not an unfalsifiable superlative — I'll leave it. Lines 417 and 543 are the two the reviewer flagged. Let me view and fix them, keeping the load-bearing emphasis in B3 while dropping the unfalsifiable ranking.Now let me verify the changes are coherent and check nothing else needs touching. Let me re-scan for superlatives and confirm the B3 anchor reference still reads well.All four points are applied and the superlatives are gone. Now let me note one downstream consistency issue: the SDK-levels line still says `compile/targetSdk = 34`, and the legacy anchor pins Compose BOM `2024.06.00` — these are consistent with each other (the Kotlin 1.9.x era) but the new lead text points at Kotlin 2.0+. I should make sure the build section doesn't now read as internally contradictory. The current structure is fine: the main bullet is toolchain-version-agnostic about SDK level (34 is still valid), and the legacy note scopes the old BOM/Kotlin combo clearly. Let me do a final full read of the build + architecture sections to confirm coherence.That reads coherently. The build section now leads with the current mechanism, keeps the known-good combination as a scoped legacy anchor, and doesn't contradict the SDK-level or dependency bullets. Let me copy to outputs and present.Done. I applied the four clear wins and left point 4 (splitting into multiple files) unactioned, keeping it a single navigable document — as discussed, that one is an optional format preference whose stated rationale rested on an assumption about the `vibe-coding/` folder that doesn't hold.

What changed:

**Point 1 (build mechanism) — verified, then reordered.** Because this is a staleness-prone toolchain detail, I searched first rather than reordering from memory. Confirmed: from Kotlin 2.0+ the Compose compiler ships with Kotlin and you apply the `org.jetbrains.kotlin.plugin.compose` plugin (version matched to Kotlin), dropping `kotlinCompilerExtensionVersion` entirely and moving compiler options to a `composeCompiler {}` block. B0 now leads with that current mechanism as the default for anything built going forward, and demotes the pinned-extension form to a clearly-scoped "legacy (pre-2.0 Kotlin only)" note — while preserving the concrete known-good combination (Kotlin 1.9.24 + extension 1.5.14 + BOM 2024.06.00) as an anchor for anyone deliberately on the old toolchain. I also added a light note that AGP 9.0+ has built-in Kotlin support.

**Point 2 (single-ViewModel scaling).** Added a "Where this pattern stops scaling" note after the reading rule in B1: the single ViewModel is right at this four-surface scale but becomes a God-object/merge-conflict magnet as independent feature areas multiply; the signal to split is surfaces *ceasing to share state*, not merely a long file, at which point scoped per-area ViewModels keep the same discipline. Framed as guidance to future readers, demanding nothing of the current app.

**Point 3 (terminology drift).** Added an "On naming" paragraph at the top of Part B: the generalized vocabulary (item/structure/workspace) is prescribed; concrete identifiers like `ItemRepository`, `notes_fts`, `docId`, `.md` are illustrative; and where a name is fixed by a platform/library (SQLite's `rowid`, `DocumentsContract` columns) that's called out explicitly.

**Point 5 (superlatives).** Removed "the single most common cause" (B7) and "the most constraint-driven part… easiest to break" (B3), keeping in both cases the concrete reason that was doing the actual persuasive work — and, per your caution, preserving B3's load-bearing "read this before changing anything here" warning rather than flattening it.

I left the trap-with-root-cause entries (B6 dp/px, B7 `SizeTransform`, B8 two-path `ParagraphStyle`), the A/B split, the reading rule, the appendix, and the FTS4 reasoning fully intact, since the reviewer and I agreed those are the document's highest-value content. The file grew modestly, from 672 to 702 lines.

_Committed as 846a2d7a659b0e341f743dd2c26314f27fdfe87b_
